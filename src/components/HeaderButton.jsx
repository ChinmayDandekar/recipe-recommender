import React from 'react'

const HeaderButton = ({ Name }) => {
    // console.log(Name)
    return (
        <li>
            <button>{ Name }</button>
        </li>
    )
}

export default HeaderButton
